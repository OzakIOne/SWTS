{
	"scripts": {
		"dev": "pnpm -F !shared run dev",
		"build": "pnpm -F !shared run build",
		"start": "pnpm -F !shared run start",
		"lint": "pnpm -F !shared run lint",
		"prettier": "pnpm -F !shared run prettier",
		"test": "pnpm -F backend run test"
	},
	"dependencies": {
		"typescript": "^5.8.3",
		"zod": "^4.0.5"
	},
	"devDependencies": {
		"oxlint": "^1.8.0",
		"prettier": "^3.6.2"
	}
}
